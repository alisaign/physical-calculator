# Physics Formula Solver

A calculator that derives and combines physics formulas to solve for a target variable based on given data.

- Uses a library of known physics equations.
- Determines how to express a target variable using available variables.
- Recursively combines and rewrites formulas to eliminate intermediate unknowns.
